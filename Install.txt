UMT部署
=======

1、解开umt.war包到webapps/ROOT(验证码要求部署在ROOT)，将
   WEB-INF/conf/umt.properties-TEMPLATE复制为umt.properties后进行修改,
   配置有关数据库名称、服务器地址、访问账户。

2、创建好数据库，不需要创建表。

3、配置email服务器。

4、启动tomcat即可。
